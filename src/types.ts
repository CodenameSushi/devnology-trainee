export interface ArticleDB {
    id: string,
    title:string,
    url:string,
    author:string,
    created_at:string
}

export interface ArticleModel {
    id:string,
    title:string,
    url:string,
    author:string,
    createdAt:string
}